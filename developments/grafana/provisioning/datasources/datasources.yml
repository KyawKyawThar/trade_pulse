# Auto-provision the Prometheus datasource so Grafana comes up wired, with no
# click-ops. Grafana loads everything under /etc/grafana/provisioning at boot.
apiVersion: 1

datasources:
- name: Prometheus
  type: prometheus
  access: proxy
  url: http://prometheus:9090
  isDefault: true
  editable: true
